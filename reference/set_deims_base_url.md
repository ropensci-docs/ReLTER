# Set DEIMS-SDR API base URL

**\[stable\]**

## Usage

``` r
set_deims_base_url(url = "https://deims.org/", force = FALSE)
```

## Arguments

- url:

  A `character`. Set the base URL to DEIMS-SDR.

- force:

  A `boolean`. Default FALSE.
